{
  "name": "wechat-claude-bridge",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "node wechat-claude-bridge.mjs",
    "login": "node wechat-claude-bridge.mjs --login",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.81",
    "dotenv": "^17.3.1",
    "qrcode": "^1.5.4"
  }
}
